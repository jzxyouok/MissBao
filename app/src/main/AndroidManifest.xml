<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.missbao.ui.activity">

    <application
        android:name="cn.missbao.golbal.BaseApplication"
        android:allowBackup="true"
        android:icon="@drawable/icon_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" >

        </activity>
        <activity android:name=".PeelingActivity" />
        <activity android:name=".StudyActivity" />
        <activity android:name=".ClockActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LogoActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".HistoryActivity"></activity>

        <service android:name="cn.missbao.service.ClockService"
            android:enabled="true"
            android:process=":push"/>

    </application>

</manifest>
